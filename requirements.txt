# Python environment for "Deep Learning From Scratch".
# Create a venv and install:  python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt
#
# From-scratch teaching code uses only NumPy; matplotlib is for quick/inline plots;
# manim renders the narrated animations. Jupyter runs each module's notebook.ipynb.

# --- core: from-scratch numerics + plots ---
numpy
matplotlib

# --- animation ---
manim

# --- notebooks (one notebook.ipynb per module) ---
jupyterlab
nbconvert
ipykernel
ipywidgets            # optional in-notebook sliders; notebooks run without it too

# --- optional: PyTorch validation cells only (never the teaching implementation) ---
# torch
